interface:
  display_name: "Loop Review"
  short_description: "Review and fix changes in isolated environments"
  default_prompt: "Use $loop-review to review and fix uncommitted changes."

policy:
  allow_implicit_invocation: false
