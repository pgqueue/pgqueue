interface:
  display_name: "Loop Review"
  short_description: "Iteratively review and fix uncommitted changes"
  default_prompt: "Use $loop-review to review and fix my uncommitted changes with fresh subagents until they meet the quality threshold."

policy:
  allow_implicit_invocation: false
